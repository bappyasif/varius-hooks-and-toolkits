{
  "compilerOptions": {
    // this use of baseUrl allows us to do absolute import
    "baseUrl": ".",
    "paths": {
      // this way we can even optimize import paths further, by use of @ for pointing to project root directory
      "@/*": ["./*"]
    }
  }
}
