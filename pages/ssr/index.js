import React from 'react'

/**
 * 
 * server-side rendering
 * ssr is a form of pre-rendering where html is generated at "request time"
 * ssr is required when we need to fetch data per request and also fetching personalized data keeping SEO in mind
 * 
 */

const ServerSideRenderingDemo = () => {
  return (
    <div>ServerSideRenderingDemo</div>
  )
}

export default ServerSideRenderingDemo